const pg=require("pg"),url=require("url");try{require("dotenv").config()}catch(a){}console.info("DATABASE_URL:",process.env.DATABASE_URL);const params=url.parse(process.env.DATABASE_URL),auth=params.auth.split(":"),config={user:auth[0],password:auth[1],host:params.hostname,port:params.port,database:params.pathname.split("/")[1],ssl:"localhost"!==params.hostname};module.exports=new pg.Pool(config);